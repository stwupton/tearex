buildType:
  default: debug
  choices:
    debug:
      short: Debug
      long: Build with debugging information.
      buildType: Debug
    release:
      short: Release
      long: Optimize the resulting binaries.
      buildType: Release

platform:
  default: windows
  choices:
    windows:
      short: Windows Platform
      long: Build for the Windows platform.
      settings: 
        PLATFORM: WIN32